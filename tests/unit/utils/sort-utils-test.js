import sortUtils from 'monday-night/utils/sort-utils';
import { module, test } from 'qunit';

module('Unit | Utility | sort-utils', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = sortUtils();
    assert.ok(result);
  });
});
